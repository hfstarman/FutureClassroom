import { GameObject } from "./objects.js";

/**
 * Physics object base class
 * @typedef {GameObject} GameObject
 */

/**
 * An array of length 16 that represents a 4D matrix.
 * @typedef {number[]} matrix4d
 */

/**
 * An array of length 3 that represents a 3D vector.
 * @typedef {number[]} vector3d
 */